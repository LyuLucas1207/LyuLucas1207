export type RingConfig = {
  innerRadius: number
  outerRadius: number
  thickness: number
  ringNumber: number
  gap: number
  gapJitter: number
  randomGap: boolean
  segments: number
  color: string
  rotation: [number, number, number]
}
